export interface AnimalCriacao {
  nome: string,
  idade: number | null,
  sobre: string,
  local: string,
  peso: number | null,
  porte: string | null,
  sexo: string,
  especieId: number | null,
  racaId: number | null,
}
